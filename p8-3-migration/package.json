{
  "name": "p8-3-migration",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "P8-3 WP3 Migration toolkit: 1.0 (MySQL) -> 2.0 (Cloudflare D1) ETL. Idempotent / resumable / batch-aware / checkpoint / dry-run. No production execution.",
  "scripts": {
    "test": "node --test",
    "dry-run": "node src/cli.js --dry-run",
    "migrate": "node src/cli.js"
  },
  "engines": {
    "node": ">=22"
  }
}
